{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.py) out=$(\"$CLAUDE_PROJECT_DIR/.venv/bin/ruff\" check \"$f\" 2>&1) || { echo \"$out\" >&2; exit 2; };; esac; }"
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.py) out=$(cd \"$CLAUDE_PROJECT_DIR\" && .venv/bin/python -m pytest -q 2>&1) || { echo \"$out\" >&2; exit 2; };; esac; }"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(.venv/bin/python -m pytest:*)",
      "Bash(.venv/bin/ruff check:*)",
      "Bash(.venv/bin/python:*)"
    ]
  }
}
